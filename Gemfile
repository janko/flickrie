source :rubygems

gemspec

group :test do
  gem "faraday", '0.7.6'
end

# You can run `bundle [install] --without extras` if you don't want to install them
group :extras do
  gem "debugger"
  gem "pry"
end
